{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "env": {
    "VITE_API_URL": {
      "description": "Backend API URL (e.g., https://your-backend-domain.com/api)",
      "required": false
    }
  }
}
